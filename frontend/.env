# URL do endpoint GraphQL do backend. O navegador (não o container) é quem
# faz a requisição, então precisa ser um endereço alcançável a partir da
# máquina do usuário — não um hostname interno do docker compose (ex.: "api").
VITE_GRAPHQL_URL=http://localhost:8080/graphql
